## AGENT 注意事项

1. 我使用 uv 管理和开发 python 项目，运行代码使用 `uv run`，添加依赖使用 `uv add` 或者通过修改 `pyproject.toml` 进行。
2. 请勿生成过多文档，当前项目的说明与改进输出到当前项目文件夹下的 `README.md` 文件中即可。
3. 代码尽可能精简，不过多拆分成不同文件，不需要相关的输入命令行参数设置（在代码中用一个类集中保存配置），聚焦于实现需求优化性能。